import { prisma } from "./prisma"

export async function recordGameResult(playerIds, scores, winnerId, leaderboard) 
{
	    try 
	    {
	    }
	    catch (error)
	    {
	        console.error("Error recording game result:", error);
		throw error;
	    }
}
